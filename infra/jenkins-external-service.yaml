apiVersion: v1
kind: Service
metadata:
  name: jenkins
  namespace: default
spec:
  type: ExternalName
  externalName: jenkins.jenkins.svc.cluster.local
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP