events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    keepalive_timeout  65;

    server {
        listen       80;
        server_name  localhost;

        # Worker frontend on /worker
        location /worker {
            alias /usr/share/nginx/html;
            index index.html index.htm;
            try_files $uri $uri/ /worker/index.html;
        }

        # Handle static files for worker (React built with PUBLIC_URL=/worker)
        location /worker/static/ {
            alias /usr/share/nginx/html/static/;
            expires 1y;
            add_header Cache-Control "public, immutable";
        }

        # Handle public files for worker (favicon, images, etc.)
        location ~ ^/worker/(favicon\.ico|.*\.(png|jpg|jpeg|gif|svg|ico))$ {
            alias /usr/share/nginx/html/$1;
            expires 1y;
            add_header Cache-Control "public, immutable";
        }

        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   /usr/share/nginx/html;
        }
    }
}